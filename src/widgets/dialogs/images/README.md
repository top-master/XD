
The `qtlogo-64.png` file was moved to the `doc/src/images` directory, and
may be used in articles about advantages of migrating from Qt to XD.

However, to keep things compatible the XD logo uses same file name.
