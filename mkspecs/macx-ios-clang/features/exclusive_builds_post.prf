
xcodebuild {
    # Prevent qmake from generating empty output dirs for each exclusive build,
    # as Xcode will do this by itself, and with a different name.
    QMAKE_DIR_REPLACE =
}

# Redirects to one-for-all-platforms script.
load(exclusive_builds_post)
